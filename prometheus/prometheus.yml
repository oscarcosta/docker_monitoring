global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Node Exporter
  - job_name: 'node-exporter'
    static_configs:
      - targets: ['localhost:9100']  # Node Exporter usa network_mode: host

  # cAdvisor
  - job_name: 'cadvisor'
    static_configs:
      - targets: ['localhost:8080']  # cAdvisor usa network_mode: host
